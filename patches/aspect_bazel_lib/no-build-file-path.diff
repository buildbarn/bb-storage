diff --git docs/expand_make_vars.md docs/expand_make_vars.md
index f3ca49f..851e11a 100644
--- docs/expand_make_vars.md
+++ docs/expand_make_vars.md
@@ -85,8 +85,6 @@ genrule-like substitutions of:
   - `$(RULEDIR)`: The output directory of the rule, that is, the directory
     corresponding to the name of the package containing the rule under the bin tree.
 
-  - `$(BUILD_FILE_PATH)`: ctx.build_file_path
-
   - `$(VERSION_FILE)`: ctx.version_file.path
 
   - `$(INFO_FILE)`: ctx.info_file.path
diff --git lib/private/diff_test.bzl lib/private/diff_test.bzl
index ffe5d3d..99d9047 100644
--- lib/private/diff_test.bzl
+++ lib/private/diff_test.bzl
@@ -71,7 +71,7 @@ def _diff_test_impl(ctx):
             "{fail_msg}": ctx.attr.failure_message,
             "{file1}": file1_path,
             "{file2}": file2_path,
-            "{build_file_path}": ctx.build_file_path,
+            "{package_dir}": ctx.label.package,
             "{diff_args}": " ".join([
                 shell.quote(arg)
                 for arg in ctx.attr.diff_args
diff --git lib/private/diff_test_tmpl.sh lib/private/diff_test_tmpl.sh
index d72f044..0330f21 100644
--- lib/private/diff_test_tmpl.sh
+++ lib/private/diff_test_tmpl.sh
@@ -23,7 +23,7 @@ resolve_exec_root() {
   local RUNFILES_PARENT
   RUNFILES_PARENT=$(dirname "$RUNFILES_DIR")
   local BIN_DIR
-  BIN_DIR="${RUNFILES_PARENT%$BUILD_FILE_DIR}"
+  BIN_DIR="${RUNFILES_PARENT%$PACKAGE_DIR}"
   local EXEC_ROOT
   EXEC_ROOT=$(dirname $(dirname $(dirname "${BIN_DIR}")))
 
@@ -52,7 +52,7 @@ find_file() {
 
   echo -n "$RF"
 }
-BUILD_FILE_DIR="$(dirname "{build_file_path}")"
+PACKAGE_DIR="{package_dir}"
 F1="{file1}"
 F2="{file2}"
 [[ "$F1" =~ ^external/ ]] && F1="${F1#external/}" || F1="$TEST_WORKSPACE/$F1"
diff --git lib/private/expand_variables.bzl lib/private/expand_variables.bzl
index a1f75bf..1fc4038 100644
--- lib/private/expand_variables.bzl
+++ lib/private/expand_variables.bzl
@@ -27,8 +27,6 @@ def expand_variables(ctx, s, outs = [], attribute_name = "args"):
       - `$(RULEDIR)`: The output directory of the rule, that is, the directory
         corresponding to the name of the package containing the rule under the bin tree.
 
-      - `$(BUILD_FILE_PATH)`: ctx.build_file_path
-
       - `$(VERSION_FILE)`: ctx.version_file.path
 
       - `$(INFO_FILE)`: ctx.info_file.path
@@ -73,7 +71,6 @@ def expand_variables(ctx, s, outs = [], attribute_name = "args"):
     additional_substitutions["RULEDIR"] = rule_dir
 
     # Add some additional make variable substitutions for common useful values in the context
-    additional_substitutions["BUILD_FILE_PATH"] = ctx.build_file_path
     additional_substitutions["VERSION_FILE"] = ctx.version_file.path
     additional_substitutions["INFO_FILE"] = ctx.info_file.path
     additional_substitutions["TARGET"] = "{}//{}:{}".format(
